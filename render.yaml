services:
  - type: web
    name: soccer-ratings
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    preDeployCommand: python3 app.py init-db
    startCommand: uvicorn soccer_ratings.webapp:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_DATABASE_URL
        sync: false
      - key: ADMIN_TOKEN
        sync: false
